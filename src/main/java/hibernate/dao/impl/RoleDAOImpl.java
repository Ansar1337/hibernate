package hibernate.dao.impl;

import hibernate.dao.interfaces.objects.RoleDAO;

// implementation of all DAO methods
// don't forget - we immediately get all the roles with the User object (fetch = EAGER) - this is specially implemented in the User class
// we can leave it empty for now - if methods are added here in the future (or you can just delete the class)
public class RoleDAOImpl implements RoleDAO {
}